# Desarrollo local: copiar a apps/web/.env. Postgres: infra/docker-compose.dev.yml
DATABASE_URL=postgresql://afuch:afuch@localhost:5433/afuch
CLAVE_CIFRADO=              # openssl rand -base64 32
CLAVE_HMAC=                 # openssl rand -base64 32
NEXTAUTH_SECRET=            # openssl rand -base64 48
NEXTAUTH_URL=http://localhost:3000
STORAGE_DIR=./storage
# Sin RESEND_API_KEY los correos se imprimen en consola (y se guardan aquí si se define):
# CORREO_BUZON_DIR=./.buzon
